---
- name: Install Copr Dependencies
  community.general.copr:
    name: "{{ item }}"
    state: enabled
  loop:
    - dejan/lazygit
    - scottames/ghostty

- name: Install DBeaver
  dnf:
    name: https://dbeaver.io/files/dbeaver-ce-latest-stable.x86_64.rpm
    state: latest
    disable_gpg_check: yes

- name: Install Dependencies
  dnf:
    name:
      - gh
      - git
      - zsh
      - gcc
      - curl
      - grip
      - tree
      - tidy
      - cmake
      - shfmt
      - unzip
      - pdftk
      - sqlite
      - neovim
      - rlwrap
      - direnv
      - pandoc
      - fd-find
      - ghostty
      - ripgrep
      - lazygit
      - opentofu
      - cpanminus
      - gmp-devel
      - shellcheck
      - glibc-devel
      - python3-pip
      - openssl-devel
      - libyaml-devel
      - readline-devel
      - fira-code-fonts
    state: latest
    update_cache: true
  notify:
    - event_download_ohmyzsh
    - event_install_ohmyzsh

- name: Install Perl Dependencies
  become: true
  community.general.cpanm:
    name: "{{ item }}"
  loop:
    - DBI
    - DBD::SQLite
    - App::Sqitch

- name: Install Brave
  shell: curl -fsS https://dl.brave.com/install.sh | sh
  args:
    creates: "/usr/bin/brave-browser"

- name: Install Rbenv
  shell: curl -fsSL https://rbenv.org/install.sh | bash
  args:
    creates: "/usr/bin/rbenv"

- name: Install Ohmyzsh
  block:
    - name: Install Ohmyzsh - Download Installer
      get_url:
        url: https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh
        dest: /tmp/ohmyzsh.sh
        mode: 0777
    - name: Install Ohmyzsh - Run Installer
      shell: |
        echo "Y" | sh /tmp/ohmyzsh.sh
      args:
        creates: "{{ ansible_env.HOME }}/.oh-my-zsh"
