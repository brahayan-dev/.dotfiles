---
- name: Install Dependencies
  become: "{{ ansible_facts['os_family'] == 'Archlinux' }}"
  package:
    name:
      - lua
      - luajit
      - fnlfmt
      - fennel
      - fennel-ls
      - podman
      - ripgrep
      - tree-sitter-cli
    state: latest
