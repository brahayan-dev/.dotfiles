---
- name: Install Dependencies
  become: "{{ ansible_facts['os_family'] == 'Archlinux' }}"
  package:
    name:
      - lua
      - luajit
      - fnlfmt
      - fennel
      - fennel-ls
      - podman
      - ripgrep
      - rustup
      - rust-analyzer
      - tree-sitter-cli
    state: latest
