(define-module (systems library common)
  #:export (command))

(define* (command action
                  #:optional entity
                  #:key (alias #f)
                  handler
                  environments)
  (handler))
